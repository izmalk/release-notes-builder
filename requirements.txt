jinja2>=3.1
requests>=2.28

# Used by tools/check_autolinks.py to decide what MyST's linkify extension
# would turn into a link. This is the same library myst-parser uses, so the
# check stays correct as the TLD list evolves. Not needed by
# build_release_notes.py itself.
linkify-it-py>=2.0

# Test-only: needed to run tests/, not by build_release_notes.py itself.
pytest>=7.0
